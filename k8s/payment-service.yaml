apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: investra

spec:
  type: ClusterIP

  selector:
    app: payment-service

  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082